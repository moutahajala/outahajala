This tool contains the Amazigh POS tagger(including a tokenizer): The POS tagger(using CRFs) with the tokenizer(using SVMs) are used both in this package. -This is the current POS tagger program(the tool accuracy is 93.82%). Usage: AmPOSrun.pl filename.
